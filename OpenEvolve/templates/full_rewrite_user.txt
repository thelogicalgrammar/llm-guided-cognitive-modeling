# Current Program Information
- Fitness: {fitness_score}
- Feature coordinates: {feature_coords}
- Focus areas: {improvement_areas}

The fitness above is measured on participants whose data was not used to fit the parameters, and
includes a BIC penalty for each parameter and each tuned constant. Those numbers are deliberately
not shown per experiment: a model tuned against them would make them meaningless.

{artifacts}

# Program Evolution History
{evolution_history}

# Current Program
```{language}
{current_program}
```

# Task
Rewrite the program to improve its FITNESS SCORE.
The system maintains diversity across these dimensions: {feature_dimensions}
Different solutions with similar fitness but different features are valuable.

Main improvement suggestion for this iteration: {improvement_suggestion}
Provide the complete new program code.

IMPORTANT: Make sure your rewritten program maintains the same inputs and outputs
as the original program, but with improved internal implementation.

```{language}
# Your rewritten program here
```